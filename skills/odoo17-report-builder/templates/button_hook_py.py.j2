import logging
from odoo import models

_logger = logging.getLogger(__name__)


class {{ target_model_class }}(models.Model):
    _inherit = "{{ target_model }}"

    def action_print_{{ report_key }}(self):
        """Return report action for selected record(s). Multi-record safe."""
        _logger.info("Printing report {{ report_title }} for %s record(s).", len(self))
        return self.env.ref("{{ report_action_xmlid_full }}").report_action(self)
